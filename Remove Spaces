#include <bits/stdc++.h> 
string replaceSpaces(string &str){
	string temp;
	for(int i=0;i<str.length();i++)
	{
		if(str[i]!=' ')
		{
			temp.push_back(str[i]);
		}
		else
		{
			temp+="@40";	
		}
	}
	return temp;
}
